Install the requirements

`pip install -r requirements.txt`

You can execute this by running

`python main.py`

In order to run this without errors, Rode Connect should be stored in
C drive and folder program
